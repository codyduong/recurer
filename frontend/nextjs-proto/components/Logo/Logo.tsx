'use client';

import styled from 'styled-components';

const Logo = styled.h1`
  font-size: 4rem;
`;

const LogoComponent = (): JSX.Element => {
  return <Logo>recur</Logo>;
};

export default LogoComponent;
